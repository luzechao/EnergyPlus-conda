# rattler-build output
output/
rb/

# CI-generated per-platform variant config (written by workflow before build)
recipe/conda_build_config.yaml

# pixi environments and lock files
.pixi/
pixi.lock

# CMake build artefacts (if someone runs cmake locally)
build/
build_energyplus/

# OS junk
.DS_Store
Thumbs.db

# Editor directories
.vscode/
.idea/
