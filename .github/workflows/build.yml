name: Build and publish EnergyPlus conda package

on:
  push:
    branches: [main, master]
    tags: ["v*"]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    name: Build (${{ matrix.target.platform }})
    runs-on: ${{ matrix.target.os }}

    # id-token: write is required for OIDC trusted publishing to prefix.dev.
    # Contents: read is required by actions/checkout.
    permissions:
      contents: read
      id-token: write

    strategy:
      fail-fast: false
      matrix:
        target:
          - platform: linux-64
            os: ubuntu-22.04
          - platform: osx-64
            os: macos-13          # Intel runner
          - platform: osx-arm64
            os: macos-14          # Apple Silicon runner
          - platform: win-64
            os: windows-2022

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Write a platform-specific variant config next to the recipe so that
      # rattler-build auto-discovers it.  The ${{ stdlib('c') }} Jinja call
      # in recipe.yaml requires c_stdlib (and c_stdlib_version) to be defined.
      - name: Write variant config (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          printf 'c_stdlib:\n  - sysroot\nc_stdlib_version:\n  - "2.17"\n' \
            > recipe/conda_build_config.yaml

      - name: Write variant config (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          if [[ "${{ matrix.target.platform }}" == "osx-arm64" ]]; then
            VER="11.0"
          else
            VER="10.13"
          fi
          printf 'c_stdlib:\n  - __osx\nc_stdlib_version:\n  - "%s"\nmacosx_deployment_target:\n  - "%s"\n' \
            "$VER" "$VER" > recipe/conda_build_config.yaml

      - name: Write variant config (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          printf 'c_stdlib:\n  - m2w64-msvcrt\n' > recipe/conda_build_config.yaml

      # rattler-build-action builds the recipe. We disable its internal
      # upload-artifact step (it uses a hardcoded path that doesn't match our
      # output layout) and do the upload ourselves below.
      - name: Build conda package
        uses: prefix-dev/rattler-build-action@v0.2.36
        with:
          recipe-path: recipe/recipe.yaml
          build-args: >-
            --target-platform ${{ matrix.target.platform }}
            -c conda-forge
          upload-artifact: false
      # Upload the built .conda file as a GitHub Actions artifact so it can
      # be downloaded from the Actions UI for inspection.
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: conda-pkg-${{ matrix.target.platform }}
          path: output/${{ matrix.target.platform }}/*.conda
          if-no-files-found: error

      # Upload to prefix.dev channel 'natlabrockies' via OIDC (no API key needed).
      # Trusted publisher must be configured at:
      #   https://prefix.dev/channels/natlabrockies/settings
      # with: owner=luzechao, repo=EnergyPlus-conda, workflow=build.yml
      # Only runs on pushes to the default branch or on tags — not on PRs.
      - name: Upload to prefix.dev
        if: >-
          github.event_name == 'push' &&
          (github.ref == 'refs/heads/main' ||
           github.ref == 'refs/heads/master' ||
           startsWith(github.ref, 'refs/tags/'))
        run: |
          rattler-build upload prefix -c natlabrockies output/${{ matrix.target.platform }}/*.conda
